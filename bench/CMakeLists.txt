# Benchmark harness is added in Phase 5; placeholder target keeps the
# top-level `add_subdirectory(bench)` valid from Phase 0 onward.
add_custom_target(symla_bench_placeholder COMMENT "benchmark harness lands in Phase 5")
